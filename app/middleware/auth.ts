export default defineNuxtRouteMiddleware(() => {
  // Middleware logic can be added here
  // For example, checking authentication status or redirecting users
  console.log("Auth middleware triggered");
});
